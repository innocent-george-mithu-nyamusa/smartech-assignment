[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["python3 -m pip3 install --upgrade pip3", "python3 -m pip3 install -r requirements-prod.txt"]

[start]
cmd = "uvicorn api.main:app --host 0.0.0.0 --port $PORT"
